Pour cette première phase on doit être capable de créer une bonne archi de base.
On doit être capable de comprendre et d'executer ces instructions 

gestion des var avec un label(avec int)
Mémoire
nop -> Rien (super dur à implementer)
ldc -> load un entier
stlloc -> affecte à une var (avec un label)

// Gestion des conditions
ceq -> push 1 si les élément sur la stack sont égaux
cgt -> a > b
clt	-> a < b
not
or
and

// Boucles
brtrue -> branch si l'élément sur la stack est vrai/==1
brfalse -> branch if false/==0


// Calcul
add
mul
div
sub
neg



// Début de l'API de notre appli
WRITE -> écrit sur la console
READ -> lit sur la console






